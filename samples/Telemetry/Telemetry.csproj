<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <OutputType>Exe</OutputType>
  </PropertyGroup>

  <!-- Version="*" is explained in samples/README.md. DocToolkitTelemetry shipped in 0.13.0, so
       this project could not exist until that release was published - which is the lag working,
       not a gap. -->
  <ItemGroup>
    <PackageReference Include="Ank.DocToolkit" Version="*" />
  </ItemGroup>

</Project>
