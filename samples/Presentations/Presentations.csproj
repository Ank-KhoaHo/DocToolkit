<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <OutputType>Exe</OutputType>
  </PropertyGroup>

  <ItemGroup>
    <PackageReference Include="Ank.DocToolkit" Version="*" />
  </ItemGroup>

  <!--
    No fixture item, and deliberately so. This sample used to link
    tests/DocToolkit.Tests/assets/sample.pptx because the public API could not produce a deck;
    PresentationEditor.Create (0.11.0) can, so the sample builds its own input. Don't reintroduce
    a Content item pointing into the test project - it made a moved fixture surface as an opaque
    MSBuild copy error.
  -->

</Project>
