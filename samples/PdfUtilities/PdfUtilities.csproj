<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <OutputType>Exe</OutputType>
  </PropertyGroup>

  <!-- Version="*" is explained in samples/README.md. PdfEditor arrived in 0.15.0, so this project
       could not exist until that release was published - which is the lag working, not a gap. -->
  <ItemGroup>
    <PackageReference Include="Ank.DocToolkit" Version="*" />
  </ItemGroup>

</Project>
